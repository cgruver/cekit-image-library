name: ops-tools
from: image-registry.openshift-image-registry.svc:5000/cekit-images/workspace-base:ubi10
version: latest
packages:
  manager: dnf
  manager_flags: "--disableplugin=subscription-manager --setopt=tsflags=nodocs --setopt=install_weak_deps=0"
modules:
  repositories:
  - path: "../../modules"
  install:
  - name: ops-tools
